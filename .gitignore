# Claude Code local config, skills, and hook logs — not part of the project
.claude/
logs/

# .NET build output
**/bin/
**/obj/
*.user
.vs/

# Node (Spec Kit e2e extension)
**/node_modules/

# Local secrets / credentials — NEVER commit
.secrets/
*.local.env
*.secret*

# POC migration workspace (exported Jira data, journal, attachments, logs) — scratch
migration-poc/

# OS / editor cruft
.DS_Store
Thumbs.db
*.swp
